# Intermediate model artifacts are regenerated by the Step5 and Step6 notebooks.
# Keep the local copies, but do not add them to the release commit.
Step5_ModelComparison/2_Results/**/*.joblib
Step6_TabPFNExplainability/2_Results/**/*.joblib
